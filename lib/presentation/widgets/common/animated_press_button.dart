import 'package:flutter/material.dart';

class AnimatedPressButton extends StatelessWidget {
  const AnimatedPressButton({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox();
  }
}
