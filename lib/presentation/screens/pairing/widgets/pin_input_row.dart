import 'package:flutter/material.dart';

class PinInputRow extends StatelessWidget {
  const PinInputRow({super.key});

  @override
  Widget build(BuildContext context) {
    return const Row();
  }
}
