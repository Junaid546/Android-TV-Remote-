import 'package:flutter/material.dart';

class PinCountdownTimer extends StatelessWidget {
  const PinCountdownTimer({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox();
  }
}
