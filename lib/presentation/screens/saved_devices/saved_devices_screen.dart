import 'package:flutter/material.dart';

class SavedDevicesScreen extends StatelessWidget {
  const SavedDevicesScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(body: Center(child: Text('Saved Devices')));
  }
}
