import 'package:flutter/material.dart';

class SavedDeviceTile extends StatelessWidget {
  const SavedDeviceTile({super.key});

  @override
  Widget build(BuildContext context) {
    return const ListTile(title: Text('Saved Device'));
  }
}
